router.get('/student/:identifier/{{prop_name}}', function(req, res) {
	Student.findById(req.params.identifier, function(err, doc) {
		if (err)
			res.status(400).send();
		else
			if(doc)
				res.status(200).json(doc.{{prop_name}});
			else
				res.status(404).send();
	});
});
