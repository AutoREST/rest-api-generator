router.delete('/{{resource_name}}/:identifier', function(req, res) {
	{{model_name}}.findByIdAndRemove(req.params.identifier, {}, function(err, doc) {
		if (err)
			res.status(400).send();
		else
			res.status(204).send();
	});
});
